# 수납 신호 관측 빌드 — 2026-09-06

## 구현과 검증 결과

- 설계 010 → 태스크 15 → 순수 판독기 → 테스트 → 기존 수신부 진단 순서로 진행했다.
- `sh test-core.sh`: 262233 assertions 통과. 기존 65개와 길이/header 경계 및
  65536개 바이트 쌍 각각의 4가지 속성 검사를 포함한다. fixture는 합성이며 Pro 3 실측이 아니다.
- `sh build.sh`: 성공. 기존 Java 8 bootstrap/deprecated API 경고는 남아 있다.
- `sh verify-apk.sh`: 서명과 권한 정책 검사 통과. 인터넷·녹음·위치·접근성 권한을 추가하지 않았다.
- 연결된 SM-F971N에 `adb install -r` 성공. 앱 데이터 삭제 없이 갱신했다.
- 기기 `lastUpdateTime`: 2026-09-06 14:38:47.
- APK SHA-256: `1540c3ddd28e6d074ed0d00997e3104733125ed885de30d201a16607d2386c37`.
- 설치 뒤 진단 prefs를 `run-as`로 읽으려 했지만 비디버그 패키지라 거부됐다.
  보안 설정을 바꾸지 않았고 카운터가 0인지 수신됐는지는 확인하지 못했다.

## 결과 예측과 피드백 분석

예상한 decoder의 unknown 보존과 유한 진단 bucket은 전수 조합에서 확인했다. 공개 문서의
primary/secondary와 배터리의 left/right를 혼동하지 않도록 API 자체에 좌우 필드를 두지 않았다.
수신부는 handshake 이후 기존 보고만 집계한다. 요청 바이트·연결당 시도 수·30초 종료·카드
표시 로직은 변경하지 않았다. 단, 이것만으로 업데이트 이후 음악 비간섭까지 실측했다고 말할 수 없다.

사용자 부재 중 강제 재연결·착용·케이스 조작·음악 재생을 수행하지 않았다. 다음 자연 연결의
기존 관측창에서 신호가 들어오면 누적 진단에 남는다. 설치가 보고 수신이나 물리 상태 일치를
증명하지는 않는다. 현재 단계에서 확인된 실물 대응은 없으며, 외관 변경도 없다.

## 다음 단계의 제한

실제 좌우 수납 대응, 완충 수납, 역할 교체는 물리 관측 대기다. 누적 진단은 현재 상태나
전환 시간축이 아니다. 자동 상태별 그림에는 별도 세션/신선도 설계와 실측이 필요하며,
30초 이후 상시 추적이 된다고 약속하지 않는다. 사용자에게 지금 조작을 요구하지 않는다.
